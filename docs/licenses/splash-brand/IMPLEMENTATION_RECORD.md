# Splash brand implementation record

- `public/assets/splash/lf-signature.mp4` is an exact byte copy of the supplied final signature.
- `mix-blend-mode:screen` performs technical black-background composition without changing source frames.
- The later user instruction removes the pixel background completely. ATC, the globally interactive grid, signature and entry button are visible from the first stage frame.
- The only visible text is the requested button label `立即进入`.
- The entry button is reduced to 124×38 CSS pixels and follows the supplied 300-frame button master: dark compact pill, one-pixel neutral outline, clipped internal warm-to-cool ribbon, pointer-local flow and retraction on leave. No thick colored outer ring or unrelated scale-up treatment remains.
- No extra AudioContext, music player, generated signature, logo, version, status subtitle or automatic entry is present.
